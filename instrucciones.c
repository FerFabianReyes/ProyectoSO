


void mov(Token *var)
{    
    Token *sig = var->sig;
    char *texto = sig->textoToken;
    if (sig->tipoParam = VAR)
    {
        
    } else
    {
        int num = texto - '0';
        if (var->textoToken == "MOV")
        {
            
        }
        
    }

}